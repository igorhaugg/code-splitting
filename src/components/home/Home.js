import React from 'react';

import Header from '../header/Header';

import './Home.css';

const Home = () => (
  <main>
    <Header />
    <section className='home' />
  </main>
);

export default Home;
